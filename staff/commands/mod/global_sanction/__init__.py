"""`/mod global …` — Moddy-team global sanctions (warn / limited / suspended).

One breach usually hits several subjects at once — the offending user *and*
their server — so these commands work on a **case group** rather than on a
single case. See ``docs/GLOBAL_SANCTIONS.md``.
"""
